%%%-------------------------------------------------------------------
%%% @doc
%%%
%%% @end
%%%-------------------------------------------------------------------
-module(riak_stat_register).

%% API
-export([]).


%% TODO

% Register the modules in riak_core
% register_module(Module) ->

% ensure all registered
% ensure that pulling udp_enabled and http_enabled if true are registered.
% unregister_module?

